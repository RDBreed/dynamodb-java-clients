#by default, using maven docker plugin
#by enabling, testcontainers will startup the localstack container
testcontainers.enabled=false
aws.endpoint=http://localhost
aws.region=eu-west-1
dynamodb.port=4566
